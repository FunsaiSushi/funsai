import React from "react";
import Post from "./Post";

export const metadata = {
  title: "Funsai | Post",
  description: "Post by Funsai.",
};

const PostPage = () => {
  return <Post />;
};

export default PostPage;
